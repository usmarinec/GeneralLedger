import { Routes } from "@angular/router";
import { LedgerWorkspaceComponent } from './features/ledger-workspace/ledger-workspace'

export const routes: Routes = [
    {
        path: '',
        component: LedgerWorkspaceComponent
    }    
];
